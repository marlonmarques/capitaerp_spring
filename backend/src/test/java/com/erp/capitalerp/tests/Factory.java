package com.erp.capitalerp.tests;

import java.time.Instant;
/*
import com.erp.capitalerp.dto.ProductDTO;
import com.erp.capitalerp.entities.Category;
import com.erp.capitalerp.entities.Product;
*/
public class Factory {
/* 
    public static String createProductJson() {
        Product product = createProduct();
        return product.toString();
    }

    public static Product createProduct() {
        Product product = new Product(1L, "Phone", 800.0, "Google phone", "http://example.com/product.png", Instant.parse("2020-10-20T03:00:00Z"));
        product.getCategories().add(createCategory());
        return product;
    }

    public static ProductDTO createProductDTO() {
        Product product = createProduct();
        return new ProductDTO(product, product.getCategories());
    }

    public static Category createCategory() {
        return new Category(1L, "Electronics");
    }
    */
}
